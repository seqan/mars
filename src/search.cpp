#include "search.hpp"

namespace mars
{

void search_generator::find_motif(stemloop_motif const & motif)
{
    std::cerr << "Let's search " << motif.elements.size() << " elements.\n";
}

} // namespace mars
